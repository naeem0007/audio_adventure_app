import 'package:flutter/material.dart';

const bgcolor = Color(0xff1F212C);
const whitecolor = Color(0xffFFFFFF);
const slidecolor = Color(0xff7E70FF);
const buttoncolor = Color(0xff60E95D);
const bgDarkColor = Color(0xff070B11);

//Gradient Color
const gbgColor = LinearGradient(
    colors: [Color(0xff000428), Color(0xff004e92), Color(0xff2C5364)],
    begin: Alignment.topRight,
    end: Alignment.bottomLeft);

const gwhiteColor = LinearGradient(
    colors: [Color(0xffFFEFBA), Color(0xffFFFFFF)],
    begin: Alignment.topRight,
    end: Alignment.bottomLeft);

const gslideColor = LinearGradient(
    colors: [Color(0xff0575E6), Color(0xff021B79)],
    begin: Alignment.topRight,
    end: Alignment.bottomLeft);

const gbuttonColor = LinearGradient(
    colors: [Color(0xff11998e), Color(0xff38ef7d)],
    begin: Alignment.topRight,
    end: Alignment.bottomLeft);

const gbgDarkColor = LinearGradient(
    colors: [Color(0xff141E30), Color(0xff243B55)],
    begin: Alignment.topRight,
    end: Alignment.bottomLeft);

gbg() {
  return const LinearGradient(
      colors: [Color(0xff141E30), Color(0xff243B55)],
      begin: Alignment.topRight,
      end: Alignment.bottomLeft);
}

const logo = "assets/images/music logo design.png";
